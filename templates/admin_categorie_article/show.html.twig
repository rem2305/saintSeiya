{% extends 'base.html.twig' %}

{% block title %}CategorieArticle{% endblock %}

{% block body %}
    <h1>CategorieArticle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_article.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categorie_article.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_categorie_article_index') }}">back to list</a>

    <a href="{{ path('app_admin_categorie_article_edit', {'id': categorie_article.id}) }}">edit</a>

    {{ include('admin_categorie_article/_delete_form.html.twig') }}
{% endblock %}
