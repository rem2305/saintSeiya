{% extends 'base.html.twig' %}

{% block title %}CategorieProduit{% endblock %}

{% block body %}
    <h1>CategorieProduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_produit.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categorie_produit.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_categorie_produit_index') }}">back to list</a>

    <a href="{{ path('app_admin_categorie_produit_edit', {'id': categorie_produit.id}) }}">edit</a>

    {{ include('admin_categorie_produit/_delete_form.html.twig') }}
{% endblock %}
