{% extends 'base.html.twig' %}

{% block title %}CategorieArticle{% endblock %}

{% block body %}

    <h1>{{ categorie_article.name }}</h1>
    
    <a href="{{ path('app_categorie_article_index') }}">Retourner à la liste des catégories</a>

    {# <div class= "mt-5">

    <table class="table">
        <thead>
            <tr>
                <th>Titre de l'article</th>
                <th>Contenu de l'article</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for article in articles %}
            <tr>
                <td>{{ article.title }}</td>
                <td>{{ article.content }}</td>
                <td>
                    <a href="{{ path('app_article_show', {'id': article.id}) }}">consulter</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">aucun article trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    </div> #}


    <div class="row d-flex justify-content-center text-center">
            {% for article in articles | reverse %}
        <div class="card m-1" style="width: 18rem;">
           
            {# {% if article.image %}            
            <img src="{{ asset('imagesArticle/' ~ article.image) }}" class="card-img-top" alt="">
            {% endif %} #}

            <div class="card-body">
                <div>
                    {# <a href=""><i class="fas fa-edit mx-1 text-success"></i></a>
                    <a href="">supprimer</a> #}
                </div>
                <h5 class="card-title">{{ article.title }}</h5>
                {# <h6 class="card-subtitle mb-2 text-muted">créé le : {{ article.dateDeCreation | date("d/m/Y \à H\\hi") }}</h6>
                {% if article.dateDemodification is not null %}
                <h6 class="card-subtitle mb-2 text-muted">modifié le : {{ article.dateDemodification | date("d/m/Y \à H\\hi") }}</h6>
                {% endif %} #}
                {# <h6>{{ article.auteur }}</h6> #}
                <p class="card-text">{{ article.content | slice(0, 85) }}...</p>
                <a href="{{ path("app_categorie_article_index", {id: article.id} ) }}" class="btn btn-primary card-link">lire l'article</a>
                
            </div>
        </div>
            {% endfor %}
    </div>

{% endblock %}
