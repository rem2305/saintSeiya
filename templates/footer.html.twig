<footer class=" {# fixed-bottom #}  bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark {# fixed-bottom #}">
    
    <div class="container-fluid d-flex justify-content-center">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
        <div class="collapse navbar-collapse nav flex-column" id="navbarSupportedContent">
            <p class="text-white-50 mt-3">©Saint Seiya Infinite - tous droits réservés</p>
            <ul class="navbar-nav mb-2 ">
                <li class="nav-item"><a class="nav-link" href="{{ path('app_home') }}">Accueil</a></li>
                {# <li class="text-white-50">_</li> #}
                <li class="nav-item"><a class="nav-link" href="{{ path('app_contact') }}">Contact</a> </li>
                {# <li class="text-white-50">_</li> #}
                <li class="nav-item"><a class="nav-link" href="{{ path("mentions_legales") }}">Mentions légales</a></li>
                {# <li class="text-white-50">_</li> #}
                <li class="nav-item"><a class="nav-link" href="{{ path("politique_confidentialite") }}">Politique de Confidentialité</a></li>
                {# <li class="text-white-50">_</li> #}
                <li class="nav-item"><a class="nav-link" href="{{ path("politique_cookies") }}">Politique de cookies (EU)</a></li>
            </ul>
        </div>
    </div>
    </nav>
</footer>