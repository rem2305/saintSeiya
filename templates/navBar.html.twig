 <nav class="{# navbar navbar-expand-lg bg-body-tertiary #} navbar navbar-expand-lg navbar-dark bg-info">
  <div class="container-fluid">
    {# <a class="navbar-brand" href="{{ path('app_home') }}">Saint Seiya Infinite</a> #}
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
        </li>
        
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="{{path('app_categorie_article_index')}}" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            La saga
          </a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="{{path('app_categorie_article_show', {id:1})}}">Hypermythe</a></li>
            <li><a class="dropdown-item" href="{{path('app_categorie_article_show', {id:2})}}">Personnages</a></li>
            
          </ul>
        </li>
        
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_categorie_article_show', {id:3})}}">Actualités</a>
        </li>
        
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="{{path('app_categorie_produit_index')}}" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Boutique
          </a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="{{path('app_categorie_produit_show', {id:1})}}">Figurines</a></li>
            <li><a class="dropdown-item" href="{{path('app_categorie_produit_show', {id:2})}}">Mangas</a></li>
            <li><a class="dropdown-item" href="{{path('app_categorie_produit_show', {id:3})}}">Animés</a></li>
          </ul>
        </li>
        
        {% if   is_granted("IS_AUTHENTICATED_FULLY")  %}
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_show_favoris', {id:1})}}"><i class="fas fa-heart text-danger"></i> Mes favoris</a>
        </li>
          {% endif %}
        {# <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Mon compte
          </a>
          <ul class="dropdown-menu"> #}
          {% if   is_granted("IS_AUTHENTICATED_FULLY")  %}
            <li class="nav-item"><a class="nav-link" href="{{ path('app_logout') }}">Deconnexion</a></li>
            <li class="nav-item"><a class="nav-link" href="{{ path('app_profile_show') }}">Profil</a></li>
            {% else %}
            <li class="nav-item"><a class="nav-link" href="{{path('app_login')}}">Connexion</a></li>
            {# <li><hr class="dropdown-divider"></li> #}
            <li class="nav-item"><a class="nav-link" href="{{path('app_register')}}">Inscription</a></li>
            {% endif %}
          {# </ul>
        </li> #}

{% if  is_granted("IS_AUTHENTICATED_FULLY") and is_granted("ROLE_ADMIN") %}
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Administration
          </a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="{{path('app_admin_article_index')}}">Articles</a></li>
            <li><a class="dropdown-item" href="{{path('app_admin_produit_index')}}">Produits</a></li>
            <li><a class="dropdown-item" href="{{path('app_admin_categorie_article_index')}}">Categories d'articles</a></li>
            <li><a class="dropdown-item" href="{{path('app_admin_categorie_produit_index')}}">Categories de produits</a></li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="{{path('app_admin_user_index')}}">Utilisateurs</a></li>
            {% endif %}

          </ul>
        </li>
      </ul>
      {# j'ajoute l'action pour rediriger vers la page de recherche et je rajoute la methode GET #}
      <form class="d-flex" role="search" action="{{path('app_recherche')}}" method="GET">
      {# Ici j'ajoute le name="search" #}
        <input class="form-control me-2" name="search" type="search" placeholder="Tapez votre recherche..." aria-label="Search">
        <button class="btn btn-outline-warning" type="submit">Rechercher</button>
      </form>
    </div>
  </div>
</nav>

<div class="bg-warning text-center py-2">
<title class="text-uppercase">Saint Seiya Infinite</title>
</div>